import { Card, Typography } from "antd";

const { Title, Paragraph } = Typography;

export default function DashboardPage() {
  return (
    <Card>
      <Title level={2}>Dashboard</Title>
      <Paragraph>Dashboard overview placeholder.</Paragraph>
    </Card>
  );
}
